// Removed - this was the query-string filter model for GET api/CustomerEnquiry
// (List), which was removed as dead: the Sale Management list page reads
// through GET api/Inquiry?category=customerenquiry instead (see the
// CustomerEnquiry section's own _comment in Queries.json for the full
// history). Safe to delete this file manually - this workspace doesn't
// allow deleting files here directly.
